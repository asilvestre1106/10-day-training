<p style="color: green"><%= notice %></p>

<% content_for :title, "Tasks" %>
<div class="max-w-2x1 mx-auto">
  <h1 class="text-3xl font-bold mb-2">A Simple Task Manager</h1>
  <h3 class="text-gray-400 mb-6">
    You have <%= @total_tasks %> tasks.
  </h3>

  <%= link_to "New task", new_task_path, class: "inline-block bg-blue-500 text-white px-4 py-2 rounded mb-4" %>
  <div id="tasks">
    <% @tasks.each do |task| %>
      <%= render task %>
    <% end %>
  </div>

  
</div>